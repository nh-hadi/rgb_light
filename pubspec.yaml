name: controlled
description: "A new Flutter project for WS2812B Smart Ambient Light Control."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  app_settings: ^5.1.1 # Membuka Pengaturan Sistem WiFi Android secara langsung

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
